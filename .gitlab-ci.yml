# .gitlab-ci.yml

image: golang:1.19

build:
  parallel:
    matrix:
      - GOOS:
          - linux
          - darwin
          - windows
        FOO:
          - 1
          - 2
          - 3
        BAR:
          - a
          - b
          - c
  script:
    - echo $GOOS $FOO $BAR
